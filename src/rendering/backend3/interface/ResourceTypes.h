#pragma once

#include <cstdlib>

namespace graphics {
using TextureId       = size_t;
using ShaderParamId   = size_t;
using PipelineStateId = size_t;
using BufferId        = size_t;
using ShaderId        = size_t;
using VertexLayoutId  = size_t;

using TextureHandle       = size_t;
using ShaderParamHandle   = size_t;
using PipelineStateHandle = size_t;
using BufferHandle        = size_t;
using ShaderHandle        = size_t;
using VertexBufferHandle  = size_t;
}
